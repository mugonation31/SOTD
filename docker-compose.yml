services:
  app:
    build:
      context: ./frontend
      dockerfile: ../Dockerfile.dev
    ports:
      - "3048:3048"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
